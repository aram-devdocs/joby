// Test file to verify Husky hooks
const message = "This is a test file for Husky hooks";
const formattedCode = "Now properly formatted";

// Using the variables to avoid unused warnings
export { message, formattedCode };
